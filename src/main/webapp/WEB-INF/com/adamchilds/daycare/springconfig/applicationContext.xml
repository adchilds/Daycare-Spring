<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

    <!-- Discover POJO @Components -->
    <!-- These automatically register an AutowiredAnnotationBeanPostProcessor -->
    <context:component-scan base-package="com.adamchilds.daycare"/>

    <!-- Default configurations -->
    <context:annotation-config />
    <mvc:default-servlet-handler />
    <mvc:annotation-driven />
    <mvc:resources location="resources/" mapping="/resources/**"/>
    <tx:annotation-driven />

    <!-- Map URIs to their controllers -->
    <bean id="unAuthenticatedUrlMapping" class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <!--
        <property name="mappings">
            <props>
                <prop key="/">mainController</prop>
                <prop key="/index">mainController</prop>
                <prop key="/login">loginController</prop>
                <prop key="/signup">registrationController</prop>
                <prop key="/submit-registration">registrationController</prop>
                <prop key="/admin">administrationController</prop>
            </props>
        </property>
        -->
    </bean>

    <!-- Map logical view names to physical views -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
        <property name="prefix" value="/WEB-INF/jsp"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- Internationalization -->
    <bean id="messageSource"
          class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <property name="basename" value="classpath:messages" />
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>

    <mvc:interceptors>
        <!-- Allows changing of site language and maps changes by appending to the URI -->
        <bean id="localeChangeInterceptor"
              class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor">
            <property name="paramName" value="lang" />
        </bean>

        <bean id="globalInterceptor" class="com.adamchilds.daycare.interceptor.GlobalInterceptor"/>
    </mvc:interceptors>

    <!-- Finds the user's locale from a predefined session attribute -->
    <bean id="localeResolver"
          class="org.springframework.web.servlet.i18n.CookieLocaleResolver">
        <property name="defaultLocale" value="en" />
    </bean>

    <!-- Enable annotation-based validation using Bean Validation Framework -->
    <!-- Using these instead of vld namespace to prevent Eclipse from complaining -->
    <bean id="configurationLoader"
          class="org.springmodules.validation.bean.conf.loader.annotation.AnnotationBeanValidationConfigurationLoader"/>
    <bean id="validator" class="org.springmodules.validation.bean.BeanValidator"
          p:configurationLoader-ref="configurationLoader"/>

</beans>