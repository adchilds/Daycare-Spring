<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	                             http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	                             http://www.springframework.org/schema/security
	                             http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <http auto-config="true" use-expressions="true">
        <!-- Secure URLs -->
        <intercept-url pattern="/contact.html" access="hasRole('ROLE_ADMINISTRATOR')" />

        <!-- All other URLs -->
        <intercept-url pattern="/**" access="isAnonymous() or isAuthenticated()" />

        <!-- Form login -->
        <form-login login-page="/login.html" default-target-url="/pricing.html" authentication-failure-url="/login.html" />

        <!-- Logout -->
        <logout logout-success-url="/index.html" />
    </http>

    <authentication-manager>
        <authentication-provider>
            <password-encoder hash="sha-256" base64="true" />
            <jdbc-user-service data-source-ref="dataSource"
                users-by-username-query="SELECT username, password, enabled FROM daycare_user u WHERE username=?"
                authorities-by-username-query="SELECT u.username, ur.user_roles FROM daycare_user u, user_roles ur WHERE u.username=?, ur.id=u.id"
            />
        </authentication-provider>
    </authentication-manager>

</beans:beans>